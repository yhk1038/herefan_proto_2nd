var dom = '<%= escape_javascript(render partial: '/links/partials/link_card', collection: @links, as: :link) %>';

$('#libraries_wrapper').empty().append(dom)
    .attr('data-load','2')
    .attr('data-method','<%= @sorting_method %>');

$load_count = 2;
$method = '<%= @sorting_method %>';